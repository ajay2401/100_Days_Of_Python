# Syntax and Concepts

    <!-- Syntax: -->

    for index in list:
        print(index)

    sum(list) -> gives the sum of integers in the list

    max(list) -> gives the max value from the list

    number = [1,2,3,4]
    sum(numbers) -> 10
    max(numbers) -> 4

    for index in range(0,5) -> 0,1,2,3,4 but not 5
    
    for index in range(1, 10, 3) -> 1,4,7,10

    for j in range(1,5,1) -> 1,2,3,4 but not 5

    x = random.choice(range(10,15))
    print(x)        -> 11

# Refer to Password_Generator.py --> Hard Case section to understand below concepts

    list(string) -> will give a list. 

        list("AB") -> ["A","B"]

    "".join(array) -> Will join the array to a string.

        "".join(list) -> "AB"
